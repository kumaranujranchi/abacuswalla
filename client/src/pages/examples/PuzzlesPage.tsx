import { ThemeProvider } from "@/components/ThemeProvider";
import PuzzlesPage from "../PuzzlesPage";

export default function PuzzlesPageExample() {
  return (
    <ThemeProvider>
      <PuzzlesPage />
    </ThemeProvider>
  );
}
